{
  "name": "myaja-backend",
  "version": "1.0.0",
  "description": "Legacy mock backend for UI-only development. The real backend lives in ../Experta/api.py",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "start:legacy": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
